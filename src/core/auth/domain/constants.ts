/** TTL do convite (spec 001 — decisão aprovada). */
export const CONVITE_TTL_MS = 72 * 60 * 60 * 1000;

/** TTL da sessão BetterAuth (spec 001 — decisão aprovada; configurado no adapter). */
export const SESSAO_TTL_MS = 7 * 24 * 60 * 60 * 1000;
