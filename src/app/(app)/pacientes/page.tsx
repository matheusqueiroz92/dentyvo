import { EmConstrucao } from "@/components/layout";

export const metadata = {
  title: "Pacientes — Dentyvo",
};

export default function PacientesPage() {
  return (
    <EmConstrucao
      titulo="Pacientes"
      descricao="O cadastro e a lista de pacientes estarão disponíveis em breve. Em construção."
    />
  );
}
