import { EmConstrucao } from "@/components/layout";

export const metadata = {
  title: "Agenda — Dentyvo",
};

export default function AgendaPage() {
  return (
    <EmConstrucao
      titulo="Agenda"
      descricao="A agenda completa estará disponível em breve. Em construção."
    />
  );
}
