export const metadata = {
  title: "Admin — Dentyvo",
};

/** Placeholder do painel da plataforma (spec 009) até a UI rica. */
export default function AdminPage() {
  return (
    <main className="mx-auto max-w-3xl px-4 py-16">
      <h1 className="text-2xl font-semibold tracking-tight">Admin da plataforma</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Área do super-admin. Em construção.
      </p>
    </main>
  );
}
